function createHTML(team) {
    var Manager = ``;
    var Engineer = ``;
    var Intern = ``;
    team.array.forEach(employee => {
        if(employee.role == 'Manager') {

        } // end of if statment
    }); // end of for each loop

}